class DebugConfig {
  /// Set this to `true` to enable the SQLite Database Viewer button/menu in the app.
  /// Set to `false` when building or launching the production application to hide all access entries.
  static const bool showDatabaseViewer = false;
}

